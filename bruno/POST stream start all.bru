meta {
  name: POST stream start (all)
  type: http
  seq: 3
}

post {
  url: {{noise_stream_url}}/stream/start
  body: none
  auth: inherit
}

docs {
  Start all noise streams (white, pink, brown).
}
